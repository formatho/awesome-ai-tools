version: "2"

linters-settings:
  govet:
    disable:
      - fieldalignment
  staticcheck:
    checks:
      - all
      - -SA9003

linters:
  enable:
    - errcheck
    - gofmt
    - goimports
    - govet
    - staticcheck
    - ineffassign
    - typecheck
    - unused

issues:
  exclude-dirs:
    - vendor
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
