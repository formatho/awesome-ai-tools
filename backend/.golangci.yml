version: "2"

linters-settings:
  govet:
    disable:
      - fieldalignment

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused

issues:
  exclude-dirs:
    - vendor
    - third_party
  exclude-files:
    - ".*_test\\.go$"
